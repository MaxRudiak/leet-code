int left = 0;
for (int right = 0; right < s.length(); right++) {
    // расширяем окно: включаем s.charAt(right)

    while (окно недопустимо) {
        // сужаем окно: исключаем s.charAt(left)
        left++;
    }

    // теперь окно допустимо → можно что-то сохранять
}
